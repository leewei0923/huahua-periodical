---
import { getCollection } from 'astro:content';
import PeriodArchive from '../../components/PeriodArchive.astro';
import { getPeriodEntries } from '../../lib/publications';

const periods = getPeriodEntries(await getCollection('blog'), 'weekly');
---

<PeriodArchive kind="weekly" periods={periods} />

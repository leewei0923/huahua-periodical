---
import { SITE_TITLE } from '../consts';
import { getCurrentPeriod } from '../lib/periods';
const pathname = Astro.url.pathname;
const isActive = (path: string) => path === '/' ? pathname === '/' : pathname.startsWith(path);
const currentWeek = getCurrentPeriod('weekly');
---

<header class="site-header">
	<nav class="nav-shell" aria-label="Primary navigation">
		<a class="wordmark" href="/" aria-label={`${SITE_TITLE}首页`}>
			<span class="wordmark-cn">花花</span>
			<span class="wordmark-dot">●</span>
			<span class="wordmark-en">Journal</span>
		</a>
		<div class="nav-links">
			<a class:list={{ active: isActive('/blog') }} href="/blog">文章 <span>Stories</span></a>
			<a class:list={{ active: isActive('/weekly') || isActive('/monthly') || isActive('/yearly') }} href="/weekly">刊期 <span>Issues</span></a>
			<a class:list={{ active: isActive('/picks') }} href="/picks">精选 <span>Picks</span></a>
			<a class:list={{ active: isActive('/about') }} href="/about">关于 <span>About</span></a>
		</div>
		<div class="nav-meta">
			<span class="issue">{currentWeek.id}</span>
			<div class="language" aria-label="语言筛选"><a href="/blog/?lang=zh">中文</a><span aria-hidden="true">/</span><a href="/blog/?lang=en">EN</a></div>
		</div>
	</nav>
</header>

<style>
	.site-header { position: sticky; top: 0; z-index: 20; padding: 0 2rem; background: color-mix(in srgb, var(--bg-soft) 91%, transparent); border-bottom: 1px solid var(--border); backdrop-filter: blur(16px); }
	.nav-shell { max-width: 1280px; min-height: 66px; margin: auto; display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 2rem; }
	a { color: var(--fg); text-decoration: none; }
	.wordmark { display: flex; align-items: baseline; gap: .55rem; width: max-content; }
	.wordmark-cn { font-size: 1.15rem; font-weight: 700; letter-spacing: .08em; }
	.wordmark-dot { color: var(--accent); font-size: .55rem; }
	.wordmark-en { font-family: var(--font-mono); font-size: .65rem; letter-spacing: .18em; text-transform: uppercase; color: var(--fg-muted); }
	.nav-links { display: flex; align-self: stretch; gap: 1.4rem; }
	.nav-links a { position: relative; display: flex; align-items: center; gap: .45rem; font-size: .86rem; }
	.nav-links a span { font-family: var(--font-mono); font-size: .58rem; letter-spacing: .12em; color: var(--fg-muted); text-transform: uppercase; }
	.nav-links a::after { content: ''; position: absolute; left: 0; right: 0; bottom: -1px; height: 2px; background: var(--accent); transform: scaleX(0); transition: transform 180ms var(--ease); }
	.nav-links a:hover::after, .nav-links a.active::after { transform: scaleX(1); }
	.nav-meta { justify-self: end; display: flex; align-items: center; gap: 1rem; font-family: var(--font-mono); font-size: .6rem; letter-spacing: .12em; color: var(--fg-muted); }
	.language { display:flex;gap:.35rem;padding: .45rem .65rem; color: var(--fg); font: inherit; background: transparent; border: 1px solid var(--border-strong); }
	.language a { color:inherit; }.language a:hover { color:var(--accent); }
	@media (max-width: 760px) {
		.site-header { padding: 0 1rem; }
		.nav-shell { min-height: 58px; grid-template-columns: 1fr auto; gap: .75rem; }
		.nav-links { order: 3; grid-column: 1 / -1; min-height: 42px; justify-content: center; border-top: 1px solid var(--border); }
		.nav-links a { font-size: .78rem; }
		.nav-links a span { display: none; }
		.nav-meta .issue { display: none; }
		.wordmark-en { display: none; }
		.language { gap: .3rem; padding: .2rem 0; border: 0; letter-spacing: .08em; }
	}
</style>
